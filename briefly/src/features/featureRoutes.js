/**
 * Teammates: optional central list of feature metadata for nav or docs.
 * Live routes are registered in `src/App.js` — add your page import there, then
 * append an entry here so tooling or docs can discover it.
 *
 * Example:
 *   import TeamMemberPage from './alex/TeamMemberPage';
 *   export const featureRegistry = [
 *     { id: 'alex-dashboard', path: '/alex', label: 'Alex dashboard', Component: TeamMemberPage },
 *   ];
 */

export const featureRegistry = [];
