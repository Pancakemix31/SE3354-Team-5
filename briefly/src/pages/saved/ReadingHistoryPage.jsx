import React from 'react';
import PageShell from '../../components/PageShell';

/** Reading history — distinct from saved list in requirements. */
export default function ReadingHistoryPage() {
  return (
    <PageShell
      title="Reading history"
      description="Recently opened articles for quick return visits."
    >
      <p>Placeholder — log opens from article detail and render chronologically.</p>
    </PageShell>
  );
}
