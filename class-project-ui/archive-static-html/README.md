# Archived static version

These files are the original plain HTML/CSS/JS demo (opened directly in a browser).

The live project now runs as a **React app** in the parent folder:

```bash
cd ..
npm install
npm start
```

Then open the URL shown in the terminal (usually http://localhost:5173).
